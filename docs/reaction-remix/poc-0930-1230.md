# Reaction Remix POC — Source 09:30–12:30

> Status: **Preservation-first POC R0-R8 passed on 2026-07-14; full-video
> acceptance remains pending**.
>
> Runtime artifacts live under the gitignored
> `work/reaction-remix-poc-preservation-first/` directory. Reusable code and
> contracts live in `reaction_remix/` and `common/schema.py`.

## 1. Reference Source

The authorized source was:

```text
C:\Users\HulkBeoti\Downloads\【海外の反応】「日本なんか来るんじゃなかった！」日本旅行を楽しんだ外国人の帰国後がヤバすぎるｗｗ.mp4
```

Observed media properties:

- Full duration: `18:49.302` (`1129.302s`).
- Video: H.264, `1920x1080`, rational CFR approximately `29.97fps`.
- Audio: one AAC stereo track at `44.1kHz`.
- Requested range: `09:30–12:30`. The source-compatible fixture starts at
  `565.898667s` and is `184.201633s` long because cuts remain on source media
  boundaries.
- The range contains Japanese editorial narration and reactions in several
  languages, including English, German, Spanish, Chinese and undetermined short
  speech fragments.

## 2. Preservation-First Boundary Result

R2 uses `segment.commentary_boundary_policy=strict_or_word_edge`. A narrator
core may become commentary when both boundaries are either `full_handle` or a
safe `word_edge`. A `word_edge` must not cross a word timestamp or overlap
another content span, and it must isolate one Japanese narrator with language
and speaker confidence at least `0.90`. Speaker/language overlap remains
protected as `mixed/unknown`.

Segment algorithm v7 produced 39 blocks covering the complete `184.201633s`
timeline with zero gaps, overlaps, or blocks shorter than `80ms`:

| Block | POC-relative span | Classification | Boundary decision |
|---|---:|---|---|
| `block-0003` | `13.246031–21.720000s` | `commentary` | Both cuts are safe `word_edge`; boundary confidence `0.90`. |
| `block-0026` | `134.840000–138.480000s` | `commentary` | Both cuts are safe `word_edge`; boundary confidence `0.90`. |
| `block-0038` | `176.892063–179.612062s` | protected `mixed` | Japanese narrator overlaps a second speaker/language; preserve source A/V. |
| `block-0039` | `179.612062–184.201633s` | protected `unknown` | Tail lacks single-speaker/language evidence; preserve source A/V. |

The final two blocks intentionally retain the old narrator where it overlaps
reaction material. They are reported through
`protected_narrator_overlap_block_ids`, not counted as replaceable-commentary
leakage and not eligible for automatic repair.

## 3. Editorial and AI33 Result

Preservation-first planning retained every non-commentary block, excluded only
`block-0003` and `block-0026`, and created one replacement slot for each. The
plan predicted `176.087664s` and unique reaction speech retention `1.0`.

The final Japanese commentary was:

1. `日米、チップで温度差。`
2. `米国系なら、試すか。`

Both slots used AI33 with voice ID
`elevenlabs_QPtBgsg1dxKTQHNpHrHt`, model label
`eleven_multilingual_v2`, speed `1.0`, and no fallback. Their rendered durations
were `2.097s` and `2.456208s`; local Japanese Whisper similarity was `1.0` for
both, so no fit request or time-stretch was needed.

## 4. R8 Acceptance Evidence

`remix_qa.json` reports `status=pass`:

| Gate | Result | Required |
|---|---:|---:|
| Output duration | `176.609767s` (`0.958785x`) | `144–180s`, hard ratio `0.80–1.00` |
| Unique reaction speech retention | `1.0` | `>=0.90` |
| Protected placements checked | `35` | all reaction/mixed/unknown placements |
| Minimum audio correlation | `0.9986287014` | `>=0.98` |
| Maximum A/V lag | `0ms` | no more than one frame |
| Maximum gain delta | `0.0691078578dB` | `<=0.3dB` |
| Minimum frame similarity | `0.9950234368` | `>=0.995` |
| Commentary JA ASR similarity | `1.0` | `>=0.90` |
| Replaced-commentary narrator leakage | `0` | `0` |
| Boundary clicks / unexpected silence | `0 / 0` | `0 / 0` |
| Output peak increase | `0.1dB` | `<=0.3dB` |
| Forbidden visual operations | `0` | `0` |

QA decodes each protected placement once and samples its head, middle and tail,
including windows up to `0.5s` at edit boundaries. This prevents a clean middle
sample from hiding damage in the first or last `100ms`.

The decoded output differs from the declared timeline by `-1` video frame and
`-1318` audio samples. Both are inside the one-frame media tolerance (`1471`
samples at the source FPS/sample rate), so timeline QA passes. The output is
outside the preferred `0.85–0.90` ratio but inside the locked hard range; this
is an explicit warning caused by preserving all protected reaction material.

## 5. Visual and Audio Policy Confirmed

- Reaction, mixed and unknown placements retain source video/audio at `1.0x`
  and `0dB`; they receive no fade, ducking, limiter or crossfade.
- Commentary fades stay inside commentary placements. Demucs bed leakage repair
  may switch only the affected commentary slot to TTS-only.
- No subtitle masking or replacement subtitle layer is used.
- No scale, crop, FPS conversion, drawtext, overlay, delogo, blur, mask,
  freeze-frame or tail padding is used in the source-compatible path.
- Burned-in subtitles, mascot, logo, branding and reaction frames remain intact.

## 6. Superseded Evidence

The earlier strict run that reported zero replaceable commentary blocks used a
full-handle-only interpretation and is superseded by v7 `word_edge` safety.
Earlier render/QA results with low correlation or frame similarity are also
stale; they predate sample-rate normalization, actual frame/sample indexing and
tail probe clamping. Only the passing preservation-first artifacts named in
this document are current POC acceptance evidence.

The earlier full-source result that reported 12 commentary segments covering
11 narrator anchors predates the same boundary hardening and remains stale. It
must not be used as full-video acceptance evidence.

## 7. Next Gate

Run the complete authorized `18:49.302` source through R0-R8 and audit all 11
known narrator anchors. Each anchor must resolve to either a replaceable core or
a protected overlap with a recorded reason. Full-video QA must meet the same
reaction preservation, AI33, leakage, visual-policy, duration and decode gates.

No PR, tag or release is authorized until that full-video acceptance passes.
